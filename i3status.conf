# i3status configuration file.
# see "man i3status" for documentation.

# It is important that this file is edited as UTF-8.
# The following line should contain a sharp s:
# ß
# If the above line is not correctly displayed, fix your editor first!

general {
        colors = true
        interval = 5
        color_good = "#55ff33"
        color_degraded = "#ffff33"
        color_bad = "#ff5533"
}

#order += "ipv6"
#order += "disk /"
#order += "run_watch DHCP"
#order += "run_watch VPN"
order += "volume master"
order += "wireless wlan0"
order += "ethernet eth0"
order += "battery 0"
order += "load"
order += "tztime local"

wireless wlan0 {
        format_up = "%quality on %ip at %essid"
        format_down = "wlan"
}

ethernet eth0 {
        # if you use %speed, i3status requires root privileges
        format_up = "%speed on %ip"
        format_down = "eth"
}

battery 0 {
        format = "%status %percentage %remaining"
}

#run_watch DHCP {
#        pidfile = "/var/run/dhclient*.pid"
#}
#
#run_watch VPN {
#        pidfile = "/var/run/vpnc/pid"
#}

tztime local {
        format = "%Y-%m-%d %A %H:%M"
}

load {
        format = "%1min"
}

#disk "/" {
#        format = "%avail"
#}

volume master {
            format = "♪: %volume"
            device = "default"
            mixer = "Master"
            mixer_idx = 0
}
